/**
 * This is the database schema for testing MySQL support of Rock DAO and Active Record.
 * The database setup in config.php is required to perform then relevant tests:
 */

DROP TABLE IF EXISTS `access_assignments` CASCADE;
DROP TABLE IF EXISTS `access_roles_items` CASCADE;
DROP TABLE IF EXISTS `access_items` CASCADE;
DROP TABLE IF EXISTS `users` CASCADE;
